// Code generated by ent, DO NOT EDIT.

package ent

import (
	"blog-server/internal/ent/gamemonthlystat"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GameMonthlyStat is the model entity for the GameMonthlyStat schema.
type GameMonthlyStat struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// YearMonth holds the value of the "yearMonth" field.
	YearMonth string `json:"yearMonth,omitempty"`
	// TotalMinutes holds the value of the "totalMinutes" field.
	TotalMinutes uint32 `json:"totalMinutes,omitempty"`
	// UpdatedAt holds the value of the "updatedAt" field.
	UpdatedAt    time.Time `json:"updatedAt,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GameMonthlyStat) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case gamemonthlystat.FieldID, gamemonthlystat.FieldTotalMinutes:
			values[i] = new(sql.NullInt64)
		case gamemonthlystat.FieldYearMonth:
			values[i] = new(sql.NullString)
		case gamemonthlystat.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GameMonthlyStat fields.
func (_m *GameMonthlyStat) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case gamemonthlystat.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = uint64(value.Int64)
		case gamemonthlystat.FieldYearMonth:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field yearMonth", values[i])
			} else if value.Valid {
				_m.YearMonth = value.String
			}
		case gamemonthlystat.FieldTotalMinutes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field totalMinutes", values[i])
			} else if value.Valid {
				_m.TotalMinutes = uint32(value.Int64)
			}
		case gamemonthlystat.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updatedAt", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GameMonthlyStat.
// This includes values selected through modifiers, order, etc.
func (_m *GameMonthlyStat) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this GameMonthlyStat.
// Note that you need to call GameMonthlyStat.Unwrap() before calling this method if this GameMonthlyStat
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *GameMonthlyStat) Update() *GameMonthlyStatUpdateOne {
	return NewGameMonthlyStatClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the GameMonthlyStat entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *GameMonthlyStat) Unwrap() *GameMonthlyStat {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: GameMonthlyStat is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *GameMonthlyStat) String() string {
	var builder strings.Builder
	builder.WriteString("GameMonthlyStat(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("yearMonth=")
	builder.WriteString(_m.YearMonth)
	builder.WriteString(", ")
	builder.WriteString("totalMinutes=")
	builder.WriteString(fmt.Sprintf("%v", _m.TotalMinutes))
	builder.WriteString(", ")
	builder.WriteString("updatedAt=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// GameMonthlyStats is a parsable slice of GameMonthlyStat.
type GameMonthlyStats []*GameMonthlyStat
